# Limit V1 to the current repository

The Personal Copilot will focus V1 on a single Current Repository instead of supporting multiple local repositories. Multi-repo support is useful later, but it adds repository selection, cross-repo memory routing, and verification complexity before the core CLI, Web Research, Repository Edit, Memory Surface, and Local Issue workflow is proven.
